---
galaxy_info:
  role_name: dr_amr_prerequisites
  namespace: netapp
  author: Kamini Singh
  description: Set up prerequisites (secrets, AppVaults, Application, snapshots) on source and destination clusters for AppMirrorRelationship-based disaster recovery.
  company: NetApp
  license: Apache-2.0
  min_ansible_version: "2.16"
  galaxy_tags:
    - netapp
    - trident
    - trident_protect
    - openshift
    - kubernetes
    - kubevirt

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
