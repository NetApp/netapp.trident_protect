---
galaxy_info:
  role_name: dr_failback_establish_forward_amr
  namespace: netapp
  author: Kamini Singh
  description: Re-establish forward replication from the original primary (source) to the destination cluster after failback.
  company: NetApp
  license: Apache-2.0
  min_ansible_version: "2.16"
  galaxy_tags:
    - netapp
    - trident
    - trident_protect
    - openshift
    - kubernetes
    - kubevirt

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
