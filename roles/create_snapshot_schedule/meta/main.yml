---
galaxy_info:
  role_name: create_snapshot_schedule
  namespace: netapp
  author: Kamini Singh
  description: Create a Trident Protect Schedule resource for periodic snapshots of an Application (set of VMs).
  company: NetApp
  license: Apache-2.0
  min_ansible_version: "2.16"
  galaxy_tags:
    - netapp
    - trident
    - trident_protect
    - openshift
    - kubernetes
    - kubevirt

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
