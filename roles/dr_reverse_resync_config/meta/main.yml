---
galaxy_info:
  role_name: dr_reverse_resync_config
  namespace: netapp
  author: Kamini Singh
  description: Reverse resync the failed-over AppMirrorRelationship to re-establish replication from the new primary back to the original primary cluster.
  company: NetApp
  license: Apache-2.0
  min_ansible_version: "2.16"
  galaxy_tags:
    - netapp
    - trident
    - openshift
    - kubernetes
    - storage
    - kubevirt

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
