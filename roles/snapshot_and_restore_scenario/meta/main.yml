---
galaxy_info:
  role_name: snapshot_and_restore_scenario
  namespace: netapp
  author: Kamini Singh
  description: Restore VMs from the latest snapshot produced by a Trident Protect snapshot schedule using a SnapshotInplaceRestore.
  company: NetApp
  license: Apache-2.0
  min_ansible_version: "2.16"
  galaxy_tags:
    - netapp
    - trident
    - openshift
    - kubernetes
    - storage
    - kubevirt

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
