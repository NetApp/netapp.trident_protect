---
galaxy_info:
  role_name: dr_reverse_resync_prerequisites
  namespace: netapp
  author: Kamini Singh
  description: Validate and set up prerequisites on the new source (original destination) cluster to reverse resync the failed-over relationship.
  company: NetApp
  license: Apache-2.0
  min_ansible_version: "2.16"
  galaxy_tags:
    - netapp
    - trident
    - trident_protect
    - appmirrorrelationship
    - data_protection
    - openshift
    - kubernetes
    - storage
    - reverse_resync
    - disaster_recovery

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
