---
galaxy_info:
  role_name: dr_amr_config
  namespace: netapp
  author: Kamini Singh
  description: Create the AppMirrorRelationship (AMR) custom resource to establish replication of VMs between source and destination OpenShift clusters.
  company: NetApp
  license: Apache-2.0
  min_ansible_version: "2.16"
  galaxy_tags:
    - netapp
    - trident
    - openshift
    - kubernetes
    - storage
    - kubevirt

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
