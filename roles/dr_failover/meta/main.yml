---
galaxy_info:
  role_name: dr_failover
  namespace: netapp
  author: Kamini Singh
  description: Fail over the replicated application to the destination OpenShift cluster after a disaster on the source cluster.
  company: NetApp
  license: Apache-2.0
  min_ansible_version: "2.16"
  galaxy_tags:
    - netapp
    - trident
    - trident_protect
    - openshift
    - kubernetes
    - kubevirt

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
